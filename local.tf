# ============================================================================
# Declare frequently used local values
# ============================================================================

locals {
  environments        = ["dev", "staging", "prod"]
  default_environment = "dev"
}